# Security Audit — {{repo_name}}

**Date:** {{YYYY-MM-DD}}
**Auditor:** Satpam OWASP (Claude)
**Modes:** {{web | api | llm | all}}
**Depth:** {{quick | deep}}
**Scope:** {{path}}
**Files scanned:** {{count}}

---

## Summary

| Severity | Count |
|----------|-------|
| Critical | {{n}} |
| High     | {{n}} |
| Medium   | {{n}} |
| Low      | {{n}} |
| **Total**| **{{n}}** |

### Top categories by finding count

1. {{category}} — {{count}} findings
2. {{category}} — {{count}} findings
3. {{category}} — {{count}} findings

### Headline issues (top 3 by severity + impact)

1. **[CRITICAL]** {{one-line description}} — `{{file:line}}`
2. **[CRITICAL]** {{one-line description}} — `{{file:line}}`
3. **[HIGH]** {{one-line description}} — `{{file:line}}`

---

## Findings

> Grouped by severity (Critical → Low), then by OWASP category.

---

### [CRITICAL] {{Category ID and name}} — {{short title}}

- **File:** `{{path/to/file.ext}}:{{line}}`
- **CWE:** [CWE-{{id}}]({{cwe url}})
- **Category:** {{OWASP category link}}

**Description**
{{What's wrong, in this codebase specifically. 2-3 sentences. No generic OWASP boilerplate.}}

**Evidence**
```{{lang}}
{{code snippet showing the flaw, with file:line}}
```

**Why this matters**
{{Concrete attacker scenario in this app. What can they do?}}

**Recommended fix**
```{{lang}}
{{minimal code change demonstrating the fix}}
```

**References**
- {{OWASP page}}
- {{CWE page}}
- {{any framework-specific doc}}

---

### [HIGH] ...

(repeat per finding)

---

## Methodology

This audit walked the following rulebooks against the codebase:
- {{list of rulebooks loaded}}

For each rule:
1. **Discover** — grep/glob for relevant patterns.
2. **Verify** — read surrounding code to confirm the protection is missing (skipped on `--depth quick`).
3. **Report** — only flag confirmed issues.

### Limitations

- Static analysis only; no dynamic / runtime testing was performed.
- Business-logic vulnerabilities outside the OWASP categories are not covered.
- Findings depend on code visibility — vendored binaries, native modules, and external services are out of scope.
- {{any other limits encountered, e.g. minified files skipped, partial repo coverage}}

### Out of scope (not audited)

- Infrastructure (cloud accounts, K8s clusters, DNS) unless reflected in repo configs.
- Penetration testing of running services.
- Compliance frameworks (SOC2, HIPAA, PCI) — overlap exists but is not the focus.
- Source: {{any exclusions per --path}}

---

## Next steps

1. **Triage** — confirm Critical and High findings; assign owners.
2. **Patch** — apply fixes; rotate any exposed secrets immediately (don't just remove from repo — git history retains them).
3. **Re-audit** — re-run satpam-owasp after fixes to confirm closure.
4. **Process** — consider adding pre-commit hooks (gitleaks, semgrep) and CI gates to catch these earlier.

---

*Generated by satpam-owasp (Claude Code skill). Standards: OWASP Top 10:2025, OWASP API Security Top 10:2023, OWASP Top 10 for LLM Applications:2025.*
