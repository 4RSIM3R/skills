# Black-Box Pentest Report — {{target_host}}

**Date:** {{YYYY-MM-DD}}
**Tester:** Rampok (Claude, browser-driven)
**Target:** {{target_url}}
**Scope:** {{scope_pattern}}
**Depth:** {{quick | deep}}
**Playbooks run:** {{list}}
**Browser MCP:** {{playwright | chrome-devtools | puppeteer}}

## Authorization basis

> {{verbatim user confirmation}}
> — User, {{datetime + tz}}

---

## Executive summary

{{2-4 sentences: what was tested, what was the most important finding, overall posture (strong / mixed / weak), urgency.}}

| Severity | Count |
|----------|-------|
| Critical | {{n}} |
| High     | {{n}} |
| Medium   | {{n}} |
| Low      | {{n}} |
| Info     | {{n}} |
| **Total**| **{{n}}** |

### Headline issues

1. **[CRITICAL]** {{title}} — {{endpoint}}
2. **[HIGH]** {{title}} — {{endpoint}}
3. **[HIGH]** {{title}} — {{endpoint}}

---

## Attack surface inventory (recon output)

### Endpoints discovered
- `{{method}} {{path}}` — {{notes: auth required? form? JSON? etc.}}
- ...

### Stack fingerprint
- **Server:** {{e.g. nginx 1.24, Cloudflare}}
- **Framework:** {{e.g. Next.js 14, Django 5, Rails 7}}
- **CDN/WAF:** {{e.g. Cloudflare, AWS WAF, none detected}}
- **Auth method:** {{cookie session, JWT, OAuth, SAML}}
- **Other notable:** {{GraphQL endpoint, Swagger exposed, etc.}}

### Subdomains / related origins observed
- {{list any beyond the scoped target — flag, do not test if out of scope}}

---

## Findings

> Grouped by severity (Critical → Low), then by playbook category.

---

### [CRITICAL] {{Category}} — {{short title}}

- **Endpoint:** `{{METHOD}} {{full URL}}`
- **OWASP mapping:** {{e.g. OWASP Top 10:2025 A01, OWASP API Top 10:2023 API1}}
- **CWE:** [CWE-{{id}}]({{cwe url}})

**Description**
{{Plain-language description of the issue, specific to this app's behavior. Not generic OWASP boilerplate.}}

**Reproduction steps**
1. {{step}}
2. {{step}}
3. {{observed result}}

**Evidence**

Request:
```http
{{METHOD}} {{path}} HTTP/1.1
Host: {{host}}
Cookie: {{relevant}}
{{other relevant headers}}

{{body if applicable}}
```

Response:
```http
HTTP/1.1 {{status}} {{reason}}
{{relevant headers}}

{{relevant body snippet — TRUNCATE PII}}
```

{{Screenshot reference if applicable, e.g. "See evidence/idor-2026-04-29.png"}}

**Impact**
{{What can an attacker actually achieve? Be concrete.}}

**Recommended fix**
{{1-3 paragraph remediation. Reference framework idioms if stack is known.}}

**References**
- {{OWASP page}}
- {{CWE page}}
- {{any framework doc}}

---

### [HIGH] ...

(repeat per finding)

---

## Methodology

This was a **black-box** test conducted via browser automation. No source code was available. Approach:

1. **Recon** — spider in-scope URLs, fingerprint stack, enumerate endpoints from links/forms/JS bundles/sitemap/robots.
2. **Per playbook** — execute checklist against the recon inventory; verify candidate findings with follow-up probes.
3. **Evidence capture** — every reported finding has a request/response or screenshot.

Playbooks executed: {{list}}

### Tooling
- Browser automation only (no `sqlmap`, `ffuf`, `burp`, `nuclei`).
- Manual follow-up recommended for: {{list any "needs deeper tool" items}}

### Throttle
Requests were rate-limited to ≤2/sec to avoid impact on the target.

### Out of scope (not tested)
- Network/infrastructure attacks (DNS, BGP, packet-level)
- Physical security
- Social engineering
- Denial of service
- Subdomains outside `{{scope_pattern}}` ({{list any seen but skipped}})
- Source-code level issues (use `satpam-owasp` for that)

### Limitations
- Black-box testing only sees what the app exposes. Latent bugs in unreached code paths are not detected.
- Authentication tested with provided/observed credentials only — privilege boundaries not held by those creds were not exercised.
- {{any other limits encountered}}

---

## Manual follow-up recommended

Findings that browser-only testing flagged but couldn't fully confirm — recommend running these with dedicated tooling:

- {{e.g. "Possible blind SQLi on /search?q= — confirm with sqlmap"}}
- {{e.g. "Subdomain enumeration on *.target.com — run amass/subfinder"}}

---

## Next steps

1. **Triage** — confirm Critical and High findings; assign owners.
2. **Fix** — apply remediations; rotate any exposed credentials.
3. **Retest** — re-run rampok after fixes to confirm closure.
4. **Defense in depth** — consider WAF rules, rate-limit policies, security headers baseline.

---

*Generated by rampok (Claude Code skill). Companion to satpam-owasp for source-code audits.*
